# legacy-cadastro

**Responsabilidade:** mapear `ConnectionId` / `ScopeRef` ↔ `cadastro_clientes` (legado).

**Implementa:** `LegacyCadastroBridge` — Fase 3 ✅ (in-memory; Supabase na Fase 4).

**Dependentes:** `platform-hub/connections/` via injeção.
