# public

**Responsabilidade:** API pública estável do Platform Hub para o restante do monorepo.

**Implementa:** re-exports controlados — cresce somente quando há implementação real.

**Dependentes:** Core OS, rotas admin, bootstrap (fase seguinte ao esqueleto).
