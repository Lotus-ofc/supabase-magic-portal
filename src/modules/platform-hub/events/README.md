# events

**Responsabilidade:** namespace `integration.*` — payloads e pontos de extensão para Core EventBus.

**Implementa:** `IntegrationEventEmitterPort`, `InMemoryIntegrationEventBus`, builders de payload.

**Não integra** ao Core EventBus nesta fase — apenas adapters e coletor in-memory.
