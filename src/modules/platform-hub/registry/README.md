# registry

**Responsabilidade:** catálogo tipado de plugins (`HubRegistry`) e descoberta (`GlobPluginLoader`).

**Implementa:** `HubRegistry`, `GlobPluginLoader` — Fase 1 (concluída).

**Dependentes:** `runtime/` (F6), bootstrap (fase seguinte ao esqueleto).
