import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth Google Business no CredentialVault. */
export const GOOGLE_BUSINESS_OAUTH_CREDENTIAL_KEY = asCredentialKey("google_business:oauth:token");
