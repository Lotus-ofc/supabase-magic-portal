import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth YouTube no CredentialVault. */
export const YOUTUBE_OAUTH_CREDENTIAL_KEY = asCredentialKey("youtube:oauth:token");
