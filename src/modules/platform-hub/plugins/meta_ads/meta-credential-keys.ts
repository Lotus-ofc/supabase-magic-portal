import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth Meta no CredentialVault. */
export const META_OAUTH_CREDENTIAL_KEY = asCredentialKey("meta:oauth:token");
