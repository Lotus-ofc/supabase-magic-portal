import { createMetaAdsAdapter } from "./create-meta-ads-registration";

/** Adapter estático para GlobPluginLoader — official_api stub sem vault. */
export const MetaAdsAdapter = createMetaAdsAdapter();
