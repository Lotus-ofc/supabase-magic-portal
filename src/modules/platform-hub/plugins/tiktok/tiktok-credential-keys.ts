import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth TikTok no CredentialVault. */
export const TIKTOK_OAUTH_CREDENTIAL_KEY = asCredentialKey("tiktok:oauth:token");
