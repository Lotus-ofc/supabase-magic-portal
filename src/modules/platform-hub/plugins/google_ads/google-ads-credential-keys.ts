import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth Google Ads no CredentialVault. */
export const GOOGLE_ADS_OAUTH_CREDENTIAL_KEY = asCredentialKey("google_ads:oauth:token");
