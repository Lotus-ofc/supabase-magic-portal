import { asCredentialKey } from "../../../../../contracts/credential/credential-vault.v1";

/** Chave canônica do bundle OAuth GA4 no CredentialVault. */
export const GA4_OAUTH_CREDENTIAL_KEY = asCredentialKey("ga4:oauth:token");
