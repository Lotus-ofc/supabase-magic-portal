# plugins

**Responsabilidade:** adapters, manifests e providers por plataforma (`{key}/`).

**Implementa:** cada pasta `{key}/` via `PluginAdapterPort` — framework Fase 3; stubs Fase 1.

**Dependentes:** `registry/` (descoberta), `runtime/` (F6).
