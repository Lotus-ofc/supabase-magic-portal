# example

Plugin **Hello World** do Platform Hub — contrato mínimo, não representa Meta nem integração real.

1. Edite `example.manifest.json`
2. `npm run generate:plugin -- example`
3. `npm run validate:plugin`
